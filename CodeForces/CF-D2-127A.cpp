#include <bits.stdc++.h>

using namespace std;

int main()
{
   int i, n, k;
   double x, y, res,
   cin >> n >> k;
   for (i = 1; i <= n; i++) {
      cin >> x >> y;
   }
   for (i = 1; i <= n; i++) {
      res =
   }

}
